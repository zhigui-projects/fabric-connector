#
# Copyright SecureKey Technologies Inc. All Rights Reserved.
#
# SPDX-License-Identifier: Apache-2.0
#
#
# The network connection profile provides client applications the information about the target
# blockchain network that are necessary for the applications to interact with it. These are all
# knowledge that must be acquired from out-of-band sources. This file provides such a source.
#


#
# Schema version of the content. Used by the SDK to apply the corresponding parsing rules.
#
version: 1.0.0

#
# The client section used by GO SDK.
#
client:

  # Which organization does this application instance belong to? The value must be the name of an org
  # defined under "organizations"
  organization: Org1

  logging:
    level: debug

  # Root of the MSP directories with keys and certs.
  cryptoconfig:
    path: /Users/kaichen/Documents/projects/fabric-connector/conf/crypto-config #配置绝对路径

  # Some SDKs support pluggable KV stores, the properties under "credentialStore"
  # are implementation specific
  credentialStore:
    # [Optional]. Used by user store. Not needed if all credentials are embedded in configuration
    # and enrollments are performed elswhere.
    path: "/tmp/state-store"

    # [Optional]. Specific to the CryptoSuite implementation used by GO SDK. Software-based implementations
    # requiring a key store. PKCS#11 based implementations does not.
    cryptoStore:
      # Specific to the underlying KeyValueStore that backs the crypto key store.
      path: /tmp/fabric-client-kv-org1

  # [Optional] BCCSP config for the client. Used by GO SDK.
  BCCSP:
    security:
      enabled: true
      default:
        provider: "SW"
      hashAlgorithm: "SM3"
      algorithm: "SM2"
      softVerify: true
      level: 256

  tlsCerts:
    # [Optional]. Use system certificate pool when connecting to peers, orderers (for negotiating TLS) Default: false
    systemCertPool: true

    # [Optional]. Client key and cert for TLS handshake with peers and orderers
    client:
      key:
        path:
      cert:
        path:
#tls:
#  guomi:
#    com:
#
# [Optional]. But most apps would have this section so that channel objects can be constructed
# based on the content below. If an app is creating channels, then it likely will not need this
# section.
#
channels:
  #[Required if _default not defined; Optional if _default defined].
  # name of the channel
  smchan: #修改成对应的通道名，从页面获取

    # list of orderers designated by the application to use for transactions on this
    # channel. This list can be a result of access control ("org1" can only access "ordererA"), or
    # operational decisions to share loads from applications among the orderers.  The values must
    # be "names" of orgs defined under "organizations/peers"
    # deprecated: not recommended, to override any orderer configuration items, entity matchers should be used.
    #    orderers:
    #      - orderer.example.com

    #[Required if _default peers not defined; Optional if _default peers defined].
    # list of peers from participating orgs
    peers:
      peer0.sm.peer: #修改成对应的节点域名，从页面获取
        # [Optional]. will this peer be sent transaction proposals for endorsement? The peer must
        # have the chaincode installed. The app can also use this property to decide which peers
        # to send the chaincode install request. Default: true
        endorsingPeer: true

        # [Optional]. will this peer be sent query proposals? The peer must have the chaincode
        # installed. The app can also use this property to decide which peers to send the
        # chaincode install request. Default: true
        chaincodeQuery: true

        # [Optional]. will this peer be sent query proposals that do not require chaincodes, like
        # queryBlock(), queryTransaction(), etc. Default: true
        ledgerQuery: true

        # [Optional]. will this peer be the target of the SDK's listener registration? All peers can
        # produce events but the app typically only needs to connect to one to listen to events.
        # Default: true
        eventSource: true

#
# list of participating organizations in this network
#
organizations:
  Org1:
    mspid: sm-peerMSP #修改成组织的MSPID，从页面获取

    # This org's MSP store (absolute path or relative to client.cryptoconfig)
    cryptoPath: peerOrganizations/sm.peer/users/smzb@sm.peer/msp
    #    cryptoPath: peerOrganizations/org1.example.com/users/{username}@org1.example.com/msp

    peers:
      - peer0.sm.peer #修改成对应的节点域名，从页面获取

    # [Optional]. Certificate Authorities issue certificates for identification purposes in a Fabric based
    # network. Typically certificates provisioning is done in a separate process outside of the
    # runtime network. Fabric-CA is a special certificate authority that provides a REST APIs for
    # dynamic certificate management (enroll, revoke, re-enroll). The following section is only for
    # Fabric-CA servers.
#    certificateAuthorities:
#      - ca_peerOrg1

#
# List of orderers to send transaction and channel create/update requests to. For the time
# being only one orderer is needed. If more than one is defined, which one get used by the
# SDK is implementation specific. Consult each SDK's documentation for its handling of orderers.
#
orderers:
  orderer0.sm.orderer: #修改成对应的节点域名，从页面获取
    # [Optional] Default: Infer from hostname
    url: orderer0.sm.orderer:7050 #修改成对应的节点域名，从页面获取，端口不需要修改

    # these are standard properties defined by the gRPC library
    # they will be passed in as-is to gRPC client constructor
    grpcOptions:
      ssl-target-name-override: orderer0-sm-orderer.sm-orderer.svc.cluster.local #修改成对应的证书域名，从页面获取
      # These parameters should be set in coordination with the keepalive policy on the server,
      # as incompatible settings can result in closing of connection.
      # When duration of the 'keep-alive-time' is set to 0 or less the keep alive client parameters are disabled
      keep-alive-time: 0s
      keep-alive-timeout: 20s
      keep-alive-permit: false
      fail-fast: false
      # allow-insecure will be taken into consideration if address has no protocol defined, if true then grpc or else grpcs
      allow-insecure: false

    tlsCACerts:
      # Certificate location absolute path
      path: /Users/kaichen/Documents/projects/fabric-connector/conf/crypto-config/ordererOrganizations/sm.orderer/tlsca/tlsca.sm.orderer-cert.pem #绝对路径

  orderer1.sm.orderer: #修改成对应的节点域名，从页面获取
    # [Optional] Default: Infer from hostname
    url: orderer1.sm.orderer:32146 #修改成对应的节点域名，从页面获取，端口不需要修改

    # these are standard properties defined by the gRPC library
    # they will be passed in as-is to gRPC client constructor
    grpcOptions:
      ssl-target-name-override: orderer1-sm-orderer.sm-orderer.svc.cluster.local #修改成对应的证书域名，从页面获取
      # These parameters should be set in coordination with the keepalive policy on the server,
      # as incompatible settings can result in closing of connection.
      # When duration of the 'keep-alive-time' is set to 0 or less the keep alive client parameters are disabled
      keep-alive-time: 0s
      keep-alive-timeout: 20s
      keep-alive-permit: false
      fail-fast: false
      # allow-insecure will be taken into consideration if address has no protocol defined, if true then grpc or else grpcs
      allow-insecure: false

    tlsCACerts:
      # Certificate location absolute path
      path: /Users/kaichen/Documents/projects/fabric-connector/conf/crypto-config/ordererOrganizations/sm.orderer/tlsca/tlsca.sm.orderer-cert.pem #绝对路径

  orderer2.sm.orderer: #修改成对应的节点域名，从页面获取
    # [Optional] Default: Infer from hostname
    url: orderer2.sm.orderer:32430 #修改成对应的节点域名，从页面获取，端口不需要修改

    # these are standard properties defined by the gRPC library
    # they will be passed in as-is to gRPC client constructor
    grpcOptions:
      ssl-target-name-override: orderer2-sm-orderer.sm-orderer.svc.cluster.local #修改成对应的证书域名，从页面获取
      # These parameters should be set in coordination with the keepalive policy on the server,
      # as incompatible settings can result in closing of connection.
      # When duration of the 'keep-alive-time' is set to 0 or less the keep alive client parameters are disabled
      keep-alive-time: 0s
      keep-alive-timeout: 20s
      keep-alive-permit: false
      fail-fast: false
      # allow-insecure will be taken into consideration if address has no protocol defined, if true then grpc or else grpcs
      allow-insecure: false

    tlsCACerts:
      # Certificate location absolute path
      path: /Users/kaichen/Documents/projects/fabric-connector/conf/crypto-config/ordererOrganizations/sm.orderer/tlsca/tlsca.sm.orderer-cert.pem #绝对路径

#
# List of peers to send various requests to, including endorsement, query
# and event listener registration.
#
peers:
  peer0.sm.peer: #修改成对应的节点域名，从页面获取
    # this URL is used to send endorsement and query requests
    # [Optional] Default: Infer from hostname
    url: peer0.sm.peer:7051 #修改成对应的节点域名，从页面获取，端口不需要修改

    grpcOptions:
      ssl-target-name-override: peer0-sm-peer.sm-peer.svc.cluster.local #修改成对应的证书域名，从页面获取
      # These parameters should be set in coordination with the keepalive policy on the server,
      # as incompatible settings can result in closing of connection.
      # When duration of the 'keep-alive-time' is set to 0 or less the keep alive client parameters are disabled
      keep-alive-time: 0s
      keep-alive-timeout: 20s
      keep-alive-permit: false
      fail-fast: false
      # allow-insecure will be taken into consideration if address has no protocol defined, if true then grpc or else grpcs
      allow-insecure: false

    tlsCACerts:
      # Certificate location absolute path
      path: /Users/kaichen/Documents/projects/fabric-connector/conf/crypto-config/peerOrganizations/sm.peer/users/smzb@sm.peer/msp/tlscacerts/tlsca.sm.peer-cert.pem

  # EntityMatchers enable substitution of network hostnames with static configurations
  # so that properties can be mapped. Regex can be used for this purpose
  # UrlSubstitutionExp can be empty which means the same network hostname will be used
  # UrlSubstitutionExp can be given same as mapped peer url, so that mapped peer url can be used
  # UrlSubstitutionExp can have golang regex matchers like ${1}.local.example.${2}:${3} for pattern
  # like peer0.org1.example.com:1234 which converts peer0.org1.example.com to peer0.org1.local.example.com:1234
  # sslTargetOverrideUrlSubstitutionExp follow in the same lines as
  # SubstitutionExp for the fields gprcOptions.ssl-target-name-override respectively
# In any case mappedHost's config will be used, so mapped host cannot be empty, if entityMatchers are used
#entityMatchers:
entityMatchers:
  peer:
    - pattern: (\w*)peer0.sm.peer(\w*)  #修改成对应的节点域名，从页面获取
      urlSubstitutionExp: 39.107.82.218:32327  #修改成对应的访问地址，从页面获取，ip:port形式，将访问地址中的grpcs://去掉
      #eventUrlSubstitutionExp: localhost:7053 # 可忽略
      sslTargetOverrideUrlSubstitutionExp: peer0-sm-peer.sm-peer.svc.cluster.local #修改成对应的证书域名，从页面获取
      mappedHost: peer0.sm.peer #修改成对应的节点域名，从页面获取

  orderer:
    - pattern: (\w*)orderer0.sm.orderer(\w*) #修改成对应的节点域名，从页面获取
      urlSubstitutionExp: 39.107.82.218:32732 #修改成对应的访问地址，从页面获取，ip:port形式，将访问地址中的grpcs://去掉
      sslTargetOverrideUrlSubstitutionExp: orderer0-sm-orderer.sm-orderer.svc.cluster.local #修改成对应的证书域名，从页面获取
      mappedHost: orderer0.sm.orderer #修改成对应的节点域名，从页面获取
    - pattern: (\w*)orderer1.sm.orderer(\w*) #修改成对应的节点域名，从页面获取
      urlSubstitutionExp: 39.107.82.218:32146 #修改成对应的访问地址，从页面获取，ip:port形式，将访问地址中的grpcs://去掉
      sslTargetOverrideUrlSubstitutionExp: orderer1-sm-orderer.sm-orderer.svc.cluster.local #修改成对应的证书域名，从页面获取
      mappedHost: orderer1.sm.orderer #修改成对应的节点域名，从页面获取
    - pattern: (\w*)orderer2.sm.orderer(\w*) #修改成对应的节点域名，从页面获取
      urlSubstitutionExp: 39.107.82.218:32430 #修改成对应的访问地址，从页面获取，ip:port形式，将访问地址中的grpcs://去掉
      sslTargetOverrideUrlSubstitutionExp: orderer2-sm-orderer.sm-orderer.svc.cluster.local #修改成对应的证书域名，从页面获取
      mappedHost: orderer2.sm.orderer #修改成对应的节点域名，从页面获取

#  certificateAuthorities:
#    - pattern: (\w*)ca0(\w*)
#      urlSubstitutionExp: http://ca0:7054
#      sslTargetOverrideUrlSubstitutionExp: ca0
#      mappedHost: ca0
